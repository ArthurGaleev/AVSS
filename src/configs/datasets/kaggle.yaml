train:
  _target_: src.datasets.YandexDownload
  instance_transforms: ${transforms.instance_transforms.train}
  data_dir: /kaggle/input/audio-visual-source-separation/dla_dataset
  part: "train"
val:
  _target_: src.datasets.YandexDownload
  instance_transforms: ${transforms.instance_transforms.train}
  data_dir: /kaggle/input/audio-visual-source-separation/dla_dataset
  part: "val"
